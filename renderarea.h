#ifndef RENDERAREA_H
#define RENDERAREA_H

#include <QWidget>

class renderArea : public QWidget
{
    Q_OBJECT
public:
    explicit renderArea(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // RENDERAREA_H